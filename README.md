# Webpage — Héctor Paredes

Sitio estático multi-página, sin frameworks: `index.html` (About, solo bio corta),
`research.html`, `playlist.html`, estilos compartidos en `css/style.css`.
Tipografía: Montserrat (como zanekashner.com) + IBM Plex Mono para metadatos.

## Publicar

Repo: `hparedes-c/hparedes-c.github.io` (esta carpeta ES el clon; el sitio Jekyll viejo
quedo en el historial git). Sitio: https://hparedes-c.github.io
Actualizar = editar aqui y `git add -A && git commit -m "..." && git push`.

## Pendientes de contenido (buscar "TODO")

- `papers/jmp.pdf` — **URL estable del JMP**: `https://hparedes-c.github.io/papers/jmp.pdf`.
  Va en la primera página del paper ("Click here for the latest version") y en el
  formulario de PSE (deadline 13-sep). Reemplazar el archivo + push no cambia el link.
- `papers/jmp_slides.pdf`; `papers/cv.pdf` existe pero es de hace 6 meses (actualizar).
- Abstract final del JMP (NO usar el del texto viejo del draft) y abstracts de los 2 WP.
- Playlists: en Spotify, Share > Embed playlist, copiar el src del iframe en music.html.
- Thumbnails de figuras del JMP: exportar 2 exhibits a `img/` y descomentar en research.html.
